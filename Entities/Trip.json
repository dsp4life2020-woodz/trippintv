{
  "name": "Trip",
  "type": "object",
  "properties": {
    "video_id": {
      "type": "string",
      "description": "ID of the video being tripped"
    },
    "user_email": {
      "type": "string",
      "description": "Email of user who tripped"
    }
  },
  "required": [
    "video_id",
    "user_email"
  ],
  "rls": {
    "write": {
      "created_by": "{{user.email}}"
    }
  }
}
